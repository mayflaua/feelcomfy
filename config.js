module.exports = {
  resolve: {
    alias: {
      // WebStorm dummy
      // eslint-disable-next-line no-undef
      '@': path.resolve(__dirname, './'),
      // eslint-disable-next-line no-undef
      '~': path.resolve(__dirname, './')
    }
  }
}
